<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fernandocejas.android10.sample.presentation">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.CAMERA"/>
  <uses-permission android:name="android.permission.VIBRATE"/>

  <application
      android:name=".AndroidApplication"
      android:allowBackup="true"
      android:icon="@drawable/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/AppTheme">

    <activity
        android:name=".view.activity.MainActivity"
        android:label="@string/app_name">
    </activity>

    <activity
        android:name=".view.activity.UserListActivity"
        android:label="@string/activity_title_user_list">
    </activity>

    <activity
        android:name=".view.activity.UserDetailsActivity"
        android:label="@string/activity_title_user_details">
    </activity>

    <activity
        android:name=".view.activity.TimeLineActivity"
        android:label="@string/app_name">
    </activity>

    <activity
        android:name=".view.activity.LoginActivity"
        android:label="@string/app_name">
    </activity>

    <activity
        android:name=".view.activity.SplashActivity"
        android:label="@string/activity_title_splash">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity
        android:name=".view.activity.TweetCardActivity"
        android:label="@string/activity_title_user_list">
    </activity>
    <activity android:name=".view.activity.PostFaceActivity"/>

    <activity
        android:name=".view.activity.MainCameraActivity"
        android:label="camera"
        android:theme="@style/Theme.AppCompat">
    </activity>

    <activity
        android:name=".view.activity.TutorialActivity"
        android:label="@string/activity_title_tutrial"
        android:theme="@style/Theme.AppCompat"
        >
    </activity>


  </application>

</manifest>
